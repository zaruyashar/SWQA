@page
@model SWQA.Pages.BugReport.EditModel
@{
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Edit Your Log</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item active">
            You can edit your bug report details here.
        </li>
    </ol>
</div>

@if (Model.errorMessage.Length > 0)
{
    <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <strong>@Model.errorMessage</strong>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="close"></button>
    </div>
}


<form method="post">
    <input type="hidden" name="BugId" value="@Model.bugreport.BugId" />

    <div class="row mb-3">
        <label class="col-sm-3">Error Code</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="ErrorCode" value="@Model.bugreport.ErrorCode" />
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3">Bug Description</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="BugDescription" value="@Model.bugreport.BugDescription" />
        </div>
    </div>

    <div class="row mb-3">
        <label class="col-sm-3">Date Reported</label>
        <div class="col-sm-6">
            <input type="date" class="form-control" name="DateReported" value="@Model.bugreport.DateReported.ToString("yyyy-MM-dd")" />
        </div>
    </div>

    <div class="row mb-3">
        <div class="offset-sm-3 col-sm-3 d-grid">
            <button type="submit" class="btn btn-outline-primary">Submit</button>
        </div>
        <div class="col-sm-3 d-grid">
            <a class="btn btn-outline-danger" href="/BugReport/Index" role="button">Cancel</a>
        </div>
    </div>
</form>